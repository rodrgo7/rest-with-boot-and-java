server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: none
    show-sql: false
springdoc:
  paths-to-match: /api/**/v1/**
  swagger-ui: 
    user-root-path: true